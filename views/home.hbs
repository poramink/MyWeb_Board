<!DOCTYPE html>
<html lang="en">
{{> baseHead }}
<body>
    {{> siteHeader }}
    <section class="section">
        <div class="container">
            <h2>ยินดีตอนรับสู่เว็บ FeelFriend</h2>
            <p class="mg-bt-large">เพื่อนๆสนใจสิ่งไหน สามารถโพสในสิ่งนั้นได้เลย...</p>
            {{#each allPost}}
                <div class="box">
                    <h4><a href="/p/{{ this.id}}">{{ this.title }}</a></h4>
                    <p>
                        <span>{{ this.from }} | </span>
                        <span class="text-gray">{{ this.createdAtText}}</span>

                    </p>
                    {{#if this.CommentCount }}
                        <div>{{this.CommentCount}} คอมเมนต์</div>
                    {{else}}
                        <div>ยังไม่มีอะไร</div>
                    {{/if}}
                </div>
            {{/each}}
        </div>
    </section>
    {{> siteFooter }}
    {{> siteScrollTop}}
    {{> siteScripts}}
</body>
</html>